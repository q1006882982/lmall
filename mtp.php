<?php

/*
 * 模版替换变量
 *
 * 数据库配置
 *
 * session前缀
 *
 * input()函数默认值 null
 *
 * insert,update方法会检查字段, 设置stric=false会自动过滤非法字段, stric=true会强制检查
 *
 * $all_data = Db::name('test')
            ->paginate(1, false, ['query'=>$this->request->get()]);
 *
 * */
